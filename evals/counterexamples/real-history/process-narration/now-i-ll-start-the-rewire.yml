id: now-i-ll-start-the-rewire
source: real-telegram
labels:
- real
- process-narration
text: 'Now I''ll start the rewire. First, add a `proc_setter` callback to `ClaudeCliHarness`
  so the bridge can mirror the running proc into `state.proc` (needed by /kill, stall
  detector, shutdown).

  '
expected_violations:
- no-empty-acknowledgment
expected_clean: []
rationale: Real assistant response harvested from patchbay-relay session logs. Pre-screened
  into bucket 'process-narration'. Verify expected_violations against current active
  rules before relying on this in CI.
